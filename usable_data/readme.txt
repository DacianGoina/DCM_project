This folder contains several csv files with data.

A lot of files contain preprocessed data produced through the time (proprocessing pipeline in different states); files name: "file_name_v{}"

all_contents.csv contains all raw data used for training and testing but gathered together into a csv file (initial data from directories structure but gathered together, not in separate files)